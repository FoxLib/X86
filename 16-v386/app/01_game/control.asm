
; ----------------------------------------------------------------------
; Ожидание срабатывание следующего тика 1/18.2 таймера
; ----------------------------------------------------------------------

timer:      push    ax cx dx
@@:         mov     ah, $00
            int     1Ah
            cmp     dx, [cs:.last]
            je      @b
            mov     [cs:.last], dx
            pop     dx cx ax
            ret
.last:      dw      0
